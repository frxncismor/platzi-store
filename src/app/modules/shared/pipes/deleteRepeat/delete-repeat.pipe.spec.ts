import { DeleteRepeatPipe } from './delete-repeat.pipe';

describe('DeleteRepeatPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteRepeatPipe();
    expect(pipe).toBeTruthy();
  });
});
